export const sampleDocumentTitle = "Abstract Structures: Notes on Foundations";

export const sampleDocumentText = `# Abstract Structures: Notes on Foundations

Mathematics often begins by hiding the incidental features of an object. A line drawn in ink, a row of symbols, and a movement in a proof may look unrelated, but each can carry the same pattern when the irrelevant surface is removed.

A structure is defined as a collection of objects together with the relations and operations that make their behavior legible. The point is not to preserve every detail, but to preserve the rules that determine what can be inferred.

When a learner meets a new structure, the first useful question is not what it resembles visually. The useful question is which transformations leave the relevant relations unchanged.

A homomorphism is a map between structures that preserves the operations or relations under discussion. If two elements combine before the map is applied, the result should match what happens when each element is mapped first and then combined.

This preservation rule lets proofs move from one setting to another. Instead of recomputing every fact, the learner can check whether the structure of the argument survives the transfer.

An invariant is a property that remains fixed under an allowed transformation. Invariants are useful because they tell the reader which facts are immune to changes in representation.

For example, the exact notation for a relation may change from one book to another. The invariant is not the typography, but the dependency that the relation imposes on the objects.

A theorem in this setting usually states that a certain preservation condition forces a stable consequence. The proof then shows why the consequence cannot be broken without breaking the condition.

Proof steps should be read as commitments. Each step narrows what may happen next, and a strong reader predicts the next obligation before the author writes it.

If a proof introduces a minimal counterexample, the expected next move is to derive a smaller counterexample or show that minimality blocks the assumed failure.

Definitions are not vocabulary decorations. A definition creates a tool for prediction because it states what must remain true when the term is used later.

A quotient construction groups objects that are treated as equivalent for the current purpose. The important question is whether the original operations still make sense after objects have been grouped.

If the operation depends on the chosen representative, the quotient is not well-defined. If every representative gives the same result, the construction can support further reasoning.

Algorithms can also be read structurally. The interesting part is not only the output, but which invariant is maintained by every iteration.

In a loop proof, initialization establishes the invariant, maintenance preserves it, and termination converts it into the desired result.

The same discipline applies outside mathematics. In history, a treaty matters when it changes the available actions of later actors, not merely because it adds a date to a timeline.

A political alliance is structurally important when it changes which conflicts can remain local and which conflicts are pulled into a wider system.

The purpose of abstract reading is to pause before the explanation arrives, predict the next constraint, and then compare that prediction with the source.`;
